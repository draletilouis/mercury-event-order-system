package com.mercury.orders.tracing

import org.springframework.stereotype.Component

@Component
class TracingAspect {
    // Simplified tracing aspect - OpenTelemetry integration can be added later
    // This ensures the build works while maintaining the component structure
}
